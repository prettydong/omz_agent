# AGENTS.md

## 项目说明

本项目是一个使用 C++ 实现的、类似 Pi 的 coding agent。
目标是兼容核心 Agent 工作流。

当前基础约定：

- C++20
- CMake
- 源文件统一使用 UTF-8
- 使用 `clang-format` 格式化代码
- JSON 使用 `nlohmann/json`，不得继续扩展自研 JSON 解析器
- 终端 UI 使用 FTXUI，核心层不得依赖 FTXUI
- 在编译器支持时启用 `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion`

## 沟通与文档

- 直接陈述结论、事实、动作和验收要求。
- 使用简洁、具体的句子，每段围绕一个主题。
- 拒绝防御性铺垫、预先辩解和无关对比，删除“不是……而是……”“不代表……”等措辞。
- 判断附依据，问题说明影响，建议给出下一步。
- 用明确的适用范围、条件和验证结果表达限制。
- 交付说明写清改动、验证结果和剩余事项。

## 工程原则

- 优先使用简单、明确、容易追踪的代码。
- 保持 Agent Loop 的行为可预测、可调试。
- 明确分离模型供应商、工具、Session 存储、上下文管理和 UI。
- 完整报告模型、工具和解析错误，保持失败状态可见。
- 新增依赖须显著降低复杂度，并具有适合本项目的许可证。
- 优先保护用户数据。没有明确的策略决定时，不得覆盖文件或执行破坏性命令。
- 以可验证的正确性、故障恢复和维护成本判断质量。
- 按本项目规模采用 SQLite 的故障测试、curl 的输入边界处理和 LLVM 的小型回归用例。
- 围绕实际问题拆分职责，让每次重构有明确收益和验证依据。

## 代码目录

默认使用以下结构，调整时说明理由：

```text
src/
  core/       Agent Loop、消息、事件、状态转换
  providers/  LLM 供应商接口和具体实现
  tools/      工具接口和内置工具
  session/    Session 持久化和 JSONL 记录
  context/    Token 预算、截断和上下文压缩
  ui/         终端输入、渲染和流式输出
  main.cpp    CLI 入口和依赖装配
include/      按子系统划分的公共头文件
tests/        单元测试、集成测试和测试 fixture
```

依赖方向由外向内：`core` 独立于 `ui`；providers 和 tools 通过小型接口与 CLI 解耦。

## 命名规范

- 类型使用 `PascalCase`，例如 `AgentLoop`、`ToolCall`。
- 函数和变量使用 `snake_case`，例如 `run_turn`、`message_count`。
- 常量使用 `kPascalCase`，例如 `kMaxToolOutputBytes`。
- 私有成员使用普通 `snake_case`，不要使用前导下划线。
- 文件名使用小写 `snake_case`，例如 `agent_loop.hpp`、`jsonl_session.cpp`。
- 命名空间使用简短、稳定的小写名称，项目统一使用 `zed`，例如 `zed`、`zed::core`。

数据类型使用名词，操作使用动词。除非职责非常明确，否则避免使用 `Manager`、`Handler` 或 `Util` 这类含义宽泛的名称。

## C++ 风格

- 所有资源都使用 RAII 管理。
- 优先使用值、引用和 `std::unique_ptr`；只有确实存在共享所有权时才使用 `std::shared_ptr`。
- 只读对象使用 `const&` 传递；发生所有权转移时移动代价较高的临时对象。
- 只有在生命周期明确且非拥有语义确实有价值时，才使用 `std::string_view`。
- 头文件必须自洽，使用到的内容必须显式 include。
- 头文件中禁止使用 `using namespace`；源文件中也只有在很小的局部作用域内才允许使用。
- 保持函数职责单一。如果一个函数包含多个无关阶段，应拆分为多个函数。
- 优先使用 `enum class`、`std::optional`、`std::variant` 和强类型，避免使用特殊数值表示状态。
- 不要用异常实现普通控制流。异常只能在跨越子系统边界时使用，并且必须在边界处转换为项目明确的错误类型。
- `core` 逻辑中不要直接使用 `printf`、`std::cout` 或日志输出。应先发出类型化事件，再由 UI 或 CLI 决定如何展示。

所有变更过的 C++ 文件都必须运行 `clang-format`。

## 错误处理与可观测性

- provider、tool、parser 和持久化操作都必须明确返回成功或错误结果。
- 错误信息必须包含操作名称和足够的诊断上下文，但绝不能包含 API Key、Token 或完整的敏感文件内容。
- 区分用户取消、可重试失败、模型输出无效、工具执行失败和永久配置错误。
- 包装错误时必须保留原始错误信息。
- 使用结构化事件表示 `agent_start`、`assistant_delta`、`tool_start`、`tool_result`、`agent_end` 和错误。
- 已开始的操作必须有可追踪的终态；错误、取消和恢复路径也要遵守事件契约，避免遗漏或重复终态事件。
- 插件和 Hook 属于子系统边界：明确异常、取消及结果校验策略；扩展修改后的数据必须重新满足宿主约束。

## Agent Loop

- 模型不得隐式执行工具；所有工具调用必须经过 `ToolRegistry`。
- 执行前必须验证工具名称和参数。
- 按顺序将 assistant 的 tool call 和 tool result 写入 Session 状态。
- 工具输出必须限制字节数，并明确标记截断内容。
- 在模型流式输出、工具执行和 Session 写入阶段都应支持取消。
- 对应操作成功前，不得修改对话状态。
- 依据协议字段和显式状态判断回合完成。
- 工具可能已产生外部副作用但结果尚未落盘；恢复时标记“结果未知”，不得自动重放写文件、Shell 等有副作用的调用。

## 工具与安全

- 每个工具都应提供精简且明确的 schema：名称、描述、输入 schema、执行函数和结果类型。
- 文件工具必须相对于明确的 workspace root 解析路径；启用 sandbox 时必须拒绝路径穿越。
- Shell 执行必须具备明确的超时时间、工作目录、环境变量策略、输出限制和取消机制。
- 默认情况下不得记录凭证，也不得将敏感信息自动传递给子进程。
- 新增工具优先验证正常调用和最关键的失败路径；按本次实现涉及的风险选择无效输入、权限失败、超时、取消或超大输出用例。
- 参数类型、数值范围和资源上限必须在转换、分配或执行之前校验；schema 声明与运行时校验必须一致。
- 分别设置输出上限和执行资源上限；读取、遍历、解析和子进程各自设置适用的字节、数量、时间和取消边界。
- 文件路径检查必须处理检查与使用之间的替换竞态，并明确路径规范化和文件系统隔离各自的保证范围。
- 工具白名单、路径限制和进程隔离是不同保证，文档必须明确各自范围。同进程原生插件属于受信任代码。

## Provider 与协议边界

- 供应商专用 JSON 逻辑必须放在 `providers/` 中；`core` 类型必须保持与供应商无关。
- 增量处理流式响应，在 delta 可安全发出时立即发送。
- 拒绝执行格式错误的 tool call，并返回模型错误。
- 重试次数必须有限、可观测，并且只对可重试错误执行。
- 配置必须来自明确的 CLI 或配置输入；不得静默切换到其他模型或 provider。

## 持久化与上下文

- 除非另有设计决定，Session 记录使用只追加的 JSONL 格式。
- 在有意义的状态边界执行 flush，使进程中断后可以恢复。
- 分别定义进程中断恢复与断电持久性要求，说明 `flush` 和 `fsync` 的使用位置，并验证相应保证。
- 持久化数据使用稳定的记录名称和版本字段。
- 上下文压缩必须是确定性的，并报告移除了哪些内容；不得移除最新用户请求或尚未处理的 tool result。

## 测试规范（Beta）

- 测试范围跟随本次改动，默认选择少量直接相关的用例，优先复用已有测试。
- 简单文案、样式和低影响配置调整通过内容检查或一次手动验证即可。
- 新增核心行为或修复关键 bug 时，补充能验证行为或复现问题的最小用例。
- 文件写入、权限、Session 和进程生命周期变更，验证受影响的成功路径和关键失败路径。
- Agent Loop 和协议集成测试使用确定性的 fixture 与 fake provider。
- 测试依赖显式声明，使用临时目录和受控环境；网络交互使用本机回环 fixture，管理好超时和清理。
- 一轮相关检查通过后结束验证；仅在代码再次变化、检查失败或出现具体疑点时补测受影响部分。
- 全量测试、多平台矩阵、sanitizer、fuzz 和性能基准用于专项任务或发布前检查，按任务需要选择。
- 交付时简述实际运行的检查及结果。

## 下一阶段目标：让失败路径可验证

按以下优先级逐步推进，每次选择一个具体问题完成。阶段验收在对应专项任务收尾时执行，日常改动采用 Beta 测试规范。

### P0：统一工具和状态边界

- 在 ToolRegistry 集中落实工具参数契约、异常到明确错误类型的转换、取消检查及结果限制；内置工具与插件工具遵守相同的宿主契约。
- 明确支持的 schema 子集，注册时拒绝不支持的约束。Hook 修改调用参数后重新验证。
- 在最终持久化和交给模型之前重新校验扩展修改后的结果；覆盖超大结果、无效 UTF-8、抛出异常和取消。
- 验收：fake provider 驱动完整回合，验证非法输入不会执行工具，失败和取消有唯一终态，Session 中工具调用与结果保持一致。

### P0：保护文件和恢复状态

- 文件工具逐级相对于已打开的 workspace 目录描述符解析路径，明确符号链接策略；读、写、编辑对每一级路径使用一致的策略。
- 明确 Session 追加失败、部分写入、尾记录损坏及工具执行后中断的恢复行为；不可恢复的数据损坏必须可见且保留原文件。
- 验收：覆盖目录替换竞态、权限失败、磁盘写入故障、执行中取消和中断恢复；证明不会越界访问或自动重复执行副作用。

### P1：把工程规则变成自动检查

- CI 增加 clang-format 检查、Linux GCC/Clang 和 macOS 构建测试，以及适用的 Debug ASan/UBSan 检查。
- 为供应商流解析、工具参数和 Session 记录增加 fuzz 入口，设置明确资源限制；发现的问题保存为确定性的回归 fixture。
- 增加安装后的 fake provider 端到端测试，验证实际安装的二进制版本和行为。
- 验收：检查可复现，失败有诊断；修复失败原因，保留用例和断言强度。

### P1：建立维护和性能基线

- 按独立变化的职责逐步拆分配置、供应商协议和插件生命周期；让 CMake 目标体现 core 与 app/UI 的依赖边界。
- 为长 Session、大工具输出、取消响应及无 Hook 的常规路径建立可重复基准；先记录基线，再决定优化。
- 补充构建、测试和贡献说明，以及安全问题报告方式；项目许可证由维护者明确选择。
- 验收：重构保持行为测试通过；性能变更提供前后数据和正确性证据。

## 后续功能方向

候选功能按用户价值排序，随任务选择实施。先完成相关 P0 边界，再扩展有副作用的能力。

1. 改动审阅与安全撤销：按任务展示文件 diff，记录 Agent 自身的文件改动；撤销前检查文件是否被用户或其他进程继续修改，发生冲突时拒绝覆盖。撤销范围限定为已记录且可校验的文件改动；Shell 和外部系统副作用单独报告。
2. 细粒度执行授权：在现有工具白名单上增加路径、操作类型和授权作用域；提供只读模式。宿主在执行边界根据结构化权限策略强制检查授权。
3. 任务预算：统一限制主 Agent 与 Sub Agent 的累计 Token、耗时和调用次数；显示消耗与停止原因。只有价格来源明确时显示成本估算，并区分估算与实际账单。
4. 失败诊断与继续执行：展示失败阶段、已完成副作用和可选后续动作；复用现有 Session 恢复和派生机制。未知结果的有副作用调用必须显式决定是否重试。
5. 上下文检查：让用户查看实际送入模型的文件、工具结果、预算占用及压缩记录，并能保留关键约束；展示内容仍须遵守凭证保护规则。

本阶段聚焦执行可靠性、改动可控性和任务可观测性。

## 变更流程（Beta）

以小步实现、快速反馈为默认节奏：

1. 完成本次改动，统一格式化变更过的 C++ 文件。
2. C++ 改动增量构建受影响目标，并运行少量相关测试；文档改动检查内容和 diff 即可。
3. 收尾时检查一次 diff，关注凭证、用户数据和无关改动；按需同步使用说明。
4. 简述改动和验证结果，完成交付。

相关验证集中在改动完成后执行，通过后继续交付。安装在用户要求、安装逻辑变更或阶段交付需要实际试用时执行；安装后检查一次版本和相关行为。

提交围绕一个具体目标组织，必要的小型重构可随功能一起完成。
